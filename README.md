# yongcat

한국어 용언(동사/형용사)의 활용형을 생성하는 Rust 라이브러리입니다.

## 특징

- 표준국어대사전 기반으로 용언 1,721개 기본 제공
- 사용자 용언 추가 지원 (`cargo run --bin import`, [USER_DATA.md](USER_DATA.md) 참고)
- 어미 그룹별 활용형 자동 생성 (아/어 계열 모음조화 처리)
- 동음이의어 구분 (`dict_id`)
- 등급별 컴파일 (학습 등급 A/B/C 필터링)

## 현황

```rust
let yongeons = yongcat::load_yongeons();
let eomis = yongcat::load_eomis();
println!("용언: {}개", yongeons.len());  // 1721
println!("어미: {}개", eomis.len());     // 46
```

## 사용법

```rust
use yongcat::eomi::ah_eo;

let yongeons = yongcat::load_yongeons();

// 단어 문자열로 활용형 생성 (동음이의어 전체 처리)
let results = yongcat::postfix(&yongeons, "가다", &ah_eo::AYO);
for (yongeon, conjugated) in &results {
    println!("{} → {}", yongeon.base_form, conjugated);
    // 가다 → 가요
}

// 단일 용언으로 활용형 생성
let meok = &yongcat::find_yongeon(&yongeons, "먹다")[0];
let result = yongcat::postfix_word(meok, &ah_eo::ASS);
// "먹었"
```

## 어미 (46개)

`load_eomis()`로 전체 어미 목록을 로드하고, `find_eomi()`로 문자열 검색할 수 있습니다.

### ah_eo (아/어 계열, 13개)

| 상수 | 어미 | 용도 |
|------|------|------|
| `A` | 아/어 | 종결 (반말) |
| `AYO` | 아요/어요 | 종결 (해요체) |
| `ASEO` | 아서/어서 | 연결 (이유) |
| `ADO` | 아도/어도 | 연결 (양보) |
| `AYA` | 아야/어야 | 연결 (조건) |
| `ARA` | 아라/어라 | 명령 |
| `ASS` | 았/었 | 과거 시제 |
| `ASS_EOYO` | 았어요/었어요 | 과거 해요체 |
| `ASS_SEUMNIDA` | 았습니다/었습니다 | 과거 합쇼체 |
| `AJIDA` | 아지다/어지다 | 피동·상태변화 |
| `ABODA` | 아보다/어보다 | 시행 |
| `ADALLA` | 아달라/어달라 | 요청 |
| `AJUDA` | 아주다/어주다 | 수혜 |

### fixed (고정 형태, 16개)

| 상수 | 어미 | 용도 |
|------|------|------|
| `GO` | 고 | 연결 (나열) |
| `JI` | 지 | 부정 |
| `NEUN` | 는 | 관형사형 (현재) |
| `GE` | 게 | 결과 |
| `JA` | 자 | 청유 |
| `GESS_EOYO` | 겠어요 | 추측 해요체 |
| `GESS_SEUMNIDA` | 겠습니다 | 추측 합쇼체 |
| `DA` | 다 | 종결 (평서) |
| `JIMAN` | 지만 | 연결 (대조) |
| `GEONA` | 거나 | 연결 (선택) |
| `NEUNDE` | 는데 | 연결 (배경·대조) |
| `DAGA` | 다가 | 연결 (전환) |
| `DOROK` | 도록 | 연결 (목적·정도) |
| `DAMYEON` | 다면 | 연결 (가정) |
| `NA` | 나 | 의문 (반말) |
| `NYA` | 냐 | 의문 (반말, 구어) |

### plain (받침 유무, 17개)

| 상수 | 어미 | 용도 |
|------|------|------|
| `EUN` | 은/ㄴ | 관형사형 (과거) |
| `EUL` | 을/ㄹ | 관형사형 (미래) |
| `EUMYEON` | 으면/면 | 조건 |
| `EUNI` | 으니/니 | 이유 |
| `SEUMNIDA` | 습니다/ㅂ니다 | 종결 (합쇼체) |
| `EUMYEONSEO` | 으면서/면서 | 동시 |
| `EURYEOGO` | 으려고/려고 | 의도 |
| `EUSEYO` | 으세요/세요 | 높임 명령 |
| `EUREO` | 으러/러 | 목적 (이동) |
| `EUSYEOSS` | 으셨/셨 | 높임 과거 |
| `EUNIKKA` | 으니까/니까 | 이유 (강조) |
| `EULKKA` | 을까/ㄹ까 | 종결 (의문·제안) |
| `EULGE` | 을게/ㄹ게 | 종결 (약속·의지) |
| `EULLAE` | 을래/ㄹ래 | 종결 (의향, 반말) |
| `EULSSUROK` | 을수록/ㄹ수록 | 연결 (점진) |
| `EUPSIDA` | 읍시다/ㅂ시다 | 종결 (격식 청유) |
| `NEUNDA` | 는다/ㄴ다 | 동사 현재 평서 (해라체) |

## 활용 규칙

### 규칙 활용

| 규칙 | 조건 | 예시 | 상태 |
|------|------|------|------|
| 모음조화 | 양성모음(ㅏ, ㅗ) → 아, 음성모음 → 어 | 가+아요 → 가요, 먹+어요 → 먹어요 | 구현 완료 |
| ㅏ+ㅏ 탈락 | 어간 ㅏ + 어미 ㅏ | 가+아요 → 가요 | 구현 완료 |
| ㅓ+ㅓ 탈락 | 어간 ㅓ + 어미 ㅓ | 서+어요 → 서요 | 구현 완료 |
| ㅗ+ㅏ 축약 | 어간 ㅗ + 어미 ㅏ → ㅘ | 오+아요 → 와요 | 구현 완료 |
| ㅜ+ㅓ 축약 | 어간 ㅜ + 어미 ㅓ → ㅝ | 주+어요 → 줘요 | 구현 완료 |
| ㅣ+ㅓ 축약 | 어간 ㅣ + 어미 ㅓ → ㅕ | 피+어요 → 펴요 | 구현 완료 |
| ㅡ 탈락 | 어간 ㅡ + 어미 ㅓ → ㅓ | 크+어요 → 커요 | 구현 완료 |

### 불규칙 활용

| 유형 | 상태 | 예시 |
|------|------|------|
| 여불규칙 | 구현 완료 | 하+여요 → 해요 |
| ㄷ불규칙 | 구현 완료 | 걷+어요 → 걸어요 |
| ㅂ불규칙 | 구현 완료 | 돕+아요 → 도와요 |
| ㅅ불규칙 | 구현 완료 | 짓+어요 → 지어요 |
| ㅎ불규칙 | 구현 완료 | 노랗+아요 → 노래요 |
| ㄹ불규칙 | 구현 완료 | 살+ㄴ → 산 |
| 르불규칙 | 구현 완료 | 모르+아요 → 몰라요 |
| 우불규칙 | 구현 완료 | 푸+어요 → 퍼요 |
| 러불규칙 | 구현 완료 | 이르+어요 → 이르러요 |
| 으불규칙 | 구현 완료 | 잠그+아요 → 잠가요 |

## 모듈 구조

| 모듈 | 역할 |
|------|------|
| `eomi` | 어미 데이터 및 `Eomi` 열거형 (`ah_eo`, `fixed`, `plain`) |
| `irregular` | 불규칙 활용 유형별 처리 (여/ㄷ/ㅂ/ㅅ/ㅎ/ㄹ/르/우/러/으) |
| `join` | 어간-어미 접합 디스패처 (irregular → regular) |
| `merge` | 음운 축약/탈락 디스패처 (irregular → regular) |
| `syllable` | 한글 음절 분해/합성, 자모 합성 |
| `yongeon` | `Yongeon` 구조체, `moeum_joha()` 모음조화 |
| `types` | `YongeonType`, `IrregularType` 열거형 |

## 등급별 컴파일

Cargo feature로 특정 학습 등급의 용언만 포함할 수 있습니다.

```bash
cargo build --features grade-a            # A등급만 (230개)
cargo build --features "grade-a,grade-b"  # A+B등급 (863개)
cargo build                               # 전체 (1,721개)
```

## 한계

### 품사별 어미 제한

`conjugate()`는 품사 제한을 검사하지 않습니다. 형용사에 동사 전용 어미를 적용해도 오류 없이 잘못된 활용형이 생성됩니다. 품사 제한이 필요하면 `conjugate_checked()`를 사용하세요. 동사 전용 어미 12개에 대해 형용사 적용 시 `Err`을 반환합니다.

### 동음이의어 구분

1,721개 용언 중 69개 기본형(156개 용언)이 동음이의어입니다. 대부분은 품사만 다르고 활용형이 같지만, 불규칙 유형이 달라 활용형이 달라지는 경우가 있습니다.

| 기본형 | 차이 | 예시 |
|--------|------|------|
| 걷다 | ㄷ불규칙 vs 규칙 | 걸어요 (걷다02) vs 걷어요 (걷다03) |
| 묻다 | ㄷ불규칙 vs 규칙 | 물어요 (묻다03) vs 묻어요 (묻다01) |
| 이르다 | 러불규칙 vs 르불규칙 | 이르러요 (이르다01) vs 일라요 (이르다03) |

이 경우 `find_yongeon`이 동음이의어를 모두 반환하므로, `dict_id`로 구별해야 합니다.

## 라이선스

MIT
